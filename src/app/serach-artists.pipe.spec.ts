import { SerachArtistsPipe } from './serach-artists.pipe';

describe('SerachArtistsPipe', () => {
  it('create an instance', () => {
    const pipe = new SerachArtistsPipe();
    expect(pipe).toBeTruthy();
  });
});
